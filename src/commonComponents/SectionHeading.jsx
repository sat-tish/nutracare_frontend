import React from 'react'

const SectionHeading = (props) => {
  return (
  
    <h1 className="text-center text-2xl font-semibold mb-[50px]">{props.name}</h1>
    
  )
}

export default SectionHeading
